# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout, delete this file and re-run i3-config-wizard(1).

# i3 config file (v4)
#
# For a complete reference, see: https://i3wm.org/docs/userguide.html

set $mod Mod4

# Font for window titles and the status bar
font pango:FiraCode Nerd Font 8

# Startup applications
exec --no-startup-id dex --autostart --environment i3
exec --no-startup-id firefox; workspace 2; exec --no-startup-id discord; workspace 3; exec --no-startup-id spotify
exec --no-startup-id xss-lock --transfer-sleep-lock -- ~/.config/i3/gruvbox-lock.sh
exec --no-startup-id nm-applet
# Load GTK/LXAppearance theme settings
exec_always --no-startup-id bash -c 'export GTK_THEME="Gruvbox-Material-Dark"; export GTK_ICON_THEME="Gruvbox-Plus-Dark"; export XCURSOR_THEME="Bibata-Original-Classic"; export XCURSOR_SIZE=24'
exec_always --no-startup-id bash -c 'source ~/.profile 2>/dev/null || true'
exec --no-startup-id xsettingsd &


# Audio Controls using pactl (PulseAudio)
set $refresh_i3status killall -SIGUSR1 i3status
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +10% && $refresh_i3status
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -10% && $refresh_i3status
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status
exec --no-startup-id stalonetray
exec --no-startup-id volumeicon

# Mouse + $mod to move floating windows
floating_modifier $mod
tiling_drag modifier titlebar

# Terminal
bindsym $mod+Return exec alacritty

# Window management
bindsym $mod+Shift+q kill
# Application launcher (Rofi)
bindsym $mod+d exec "rofi -modi drun,run -show drun" 
bindsym Mod1+Tab exec "rofi -modi window, run -show window"
bindsym $mod+Shift+P exec "rofi -show p -modi p:'rofi-power-menu'"
# Screenshotting
bindsym Print exec --no-startup-id flameshot gui  

# Navigation and movement (focus and move)
bindsym $mod+j focus left
bindsym $mod+k focus down
bindsym $mod+l focus up
bindsym $mod+semicolon focus right

bindsym $mod+Shift+j move left
bindsym $mod+Shift+k move down
bindsym $mod+Shift+l move up
bindsym $mod+Shift+semicolon move right

# Split orientation
bindsym $mod+h split h
bindsym $mod+v split v

# Layouts and fullscreen
bindsym $mod+f fullscreen toggle



bindsym $mod+Shift+space floating toggle

# Workspace definitions
set $ws1 "1: 󰈹 firefox"
set $ws2 "2:  discord"
set $ws3 "3:  spotify"
set $ws4 "4:  terminal"
set $ws5 "5:  nvim"
set $ws6 "6:  steam"
set $ws7 "7: 󰍳 minecraft"
set $ws8 "8:  github"
set $ws9 "9: 󰰟 roblox"
set $ws10 "10: 󰯜 obs studio"

# Switch workspaces
bindsym $mod+1 workspace $ws1
bindsym $mod+2 workspace $ws2
bindsym $mod+3 workspace $ws3
bindsym $mod+4 workspace $ws4
bindsym $mod+5 workspace $ws5
bindsym $mod+6 workspace $ws6
bindsym $mod+7 workspace $ws7
bindsym $mod+8 workspace $ws8
bindsym $mod+9 workspace $ws9
bindsym $mod+0 workspace $ws10

# Move focused window to workspace
bindsym $mod+Shift+1 move container to workspace $ws1
bindsym $mod+Shift+2 move container to workspace $ws2
bindsym $mod+Shift+3 move container to workspace $ws3
bindsym $mod+Shift+4 move container to workspace $ws4
bindsym $mod+Shift+5 move container to workspace $ws5
bindsym $mod+Shift+6 move container to workspace $ws6
bindsym $mod+Shift+7 move container to workspace $ws7
bindsym $mod+Shift+8 move container to workspace $ws8
bindsym $mod+Shift+9 move container to workspace $ws9
bindsym $mod+Shift+0 move container to workspace $ws10

# Window border styles and appearance 
client.focused          #7c6f64 #7c6f64 #ffffff #1d2021   #7c6f64  # Focused border: Darker brownish gray (instead of green)
client.focused_inactive #504945 #504945 #ebdbb2 #282828   #7c6f64  # Inactive border: Dark Brownish Gray
client.unfocused        #1d2021 #1d2021 #a89984 #1d2021   #7c6f64  # Unfocused: Dark background with gray text
client.urgent           #fb4934 #fb4934 #ffffff #fb4934   #fb4934  # Urgent: Bright red

# i3bar (Status bar)
bar {
    status_command i3status
    position bottom
    font pango:FiraCode Nerd Font 10
    colors {
        background  #1d2021           # Background color (dark, hard)
        statusline  #ebdbb2           # Light foreground for status text
        separator   #a89984           # Border/Separator: Muted gray
        focused_workspace   #7c6f64 #7c6f64 #ffffff  # Focused workspace: Darker gray (instead of green)
        active_workspace    #504945 #504945 #ffffff  # Active workspace: Dark gray background
        inactive_workspace  #282828 #282828 #a89984  # Inactive workspace: Muted gray text
        urgent_workspace    #fb4934 #fb4934 #ffffff  # Urgent workspace: Red with white text
        binding_mode        #7c6f64 #7c6f64 #ffffff  # Binding mode: Dark gray (instead of green)
    }
}




# Startup applications
exec_always --no-startup-id xautolock -time 10 -locker ~/.config/i3/gruvbox-lock.sh
# Set wallpaper
exec_always --no-startup-id feh --bg-fill --no-fehbg ~/images/neckhurt.jpg
exec_always --no-startup-id pulseaudio --start
exec_always --no-startup-id lxpolkit
exec_always --no-startup-id nm-applet

bindsym $mod+Shift+o exec --no-startup-id ~/.config/i3/gruvbox-lock.sh
# Reload and restart i3
bindsym $mod+Shift+c reload
bindsym $mod+Shift+r restart
bindsym $mod+Shift+e exec --no-startup-id i3-nagbar -t warning -f "pango:Hack 9" -m "Do you really want to exit?" -B "Exit" "i3-msg exit" -B "Lock" "i3lock-fancy -f Source-Sans-Pro"

# Resize windows (using $mod + hjkl or arrow keys)
mode "resize" {
    bindsym j resize shrink width 10 px or 10 ppt
    bindsym k resize grow height 10 px or 10 ppt
    bindsym l resize shrink height 10 px or 10 ppt
    bindsym semicolon resize grow width 10 px or 10 ppt
    bindsym Left resize shrink width 10 px or 10 ppt
    bindsym Down resize grow height 10 px or 10 ppt
    bindsym Up resize shrink height 10 px or 10 ppt
    bindsym Right resize grow width 10 px or 10 ppt
    bindsym Return mode "default"
    bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

# Include local config file
include local.conf
